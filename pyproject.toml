[project]
name = "citation-listener"
version = "0.1.0"
description = "CMIP7 Publication Producer/Listener for creating Aggregations and New Citations (PLANC)"
authors = [
    {name = "Daniel Westwood",email = "daniel.westwood@stfc.ac.uk"}
]
readme = "README.md"
requires-python = ">=3.13, <4.0"
dependencies = [
    "confluent-kafka (>=2.14.0,<3.0.0)",
    "pyyaml (>=6.0.3,<7.0.0)",
    "click (>=8.3.2,<9.0.0)",
    "esgf-core-utils (>=1.0.7,<2.0.0)",
    "esgcet (>=5.3.6,<6.0.0)",
    "httpx-auth (>=0.23.1,<0.24.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "isort (>=8.0.1,<9.0.0)",
    "ruff (>=0.16.3,<0.17.0)"
]

[project.scripts]
# General listener command for kafka listening
public_listen = "citation_listener.base:main"

# Patch Citations from local list, failed list in service or both.
patch_citations = "citation_listener.patch:patch_citations"

# Patch missing cite-as links across ALL items in supported projects.
patch_stac = "citation_listener.patch:patch_stac"